export const getURL = () => {
    if(window.location.hostname === 'lorenaaguilar.github.io') {
        return 'Trabalho_Sistema_de_Servicos_Gerais_frontend';
    }
    else {
        return '';
    }
}